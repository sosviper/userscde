<div class="container p-4">
  <div class="row">
    {{!-- {{#each users }}
      <div class="col-md-3">
        <div class="card text-center">
          <div class="card-body">
            <a href="">
              <h3 class="card-title text-uppercase">{{ fullname }}</h3>
            </a>
            <p class="m-2">{{ email }}</p>
            <p class="m-2">{{ dni }}</p>
            <p class="m-2">{{ fullname_certificado }}</p>
            <p>{{ timeago created_at }}</p>
            <a href="/users/delete/{{id}}" class="btn btn-danger">Eliminar</a>
            <a href="/users/edit/{{dni}}" class="btn btn-secondary">Editar</a>
          </div>
        </div>
      </div>
    {{else}}
      <div class="col-md4 mx-auto">
        <div class="card card-body text-center">
          <p>No hay usuarios creados aún.</p>
          <a href="/users/add" class="">Crear Docente</a>
        </div>
      </div>
    {{/each}} --}}
  </div>
</div>